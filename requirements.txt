Flask==3.0.3
requests==2.32.3
gunicorn==22.0.0
# Image conversion + downsize. Pillow handles WebP / JPG decode and the
# LANCZOS resize in downsize_png(); CairoSVG renders SVG via libcairo2
# (installed in the Dockerfile).
Pillow==10.4.0
cairosvg==2.7.1
# HTML fetch + simplify for the /page endpoint. lxml ships a binary wheel
# (bundled libxml2), so no extra system packages are needed.
beautifulsoup4==4.12.3
lxml==5.2.2
